apiVersion: v1
kind: Service
metadata:
  name: {{ include "nti-app.fullname" . }}
  labels:
    {{- include "nti-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nti-app.selectorLabels" . | nindent 4 }}
